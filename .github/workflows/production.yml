on:
  release:
    types:
      - created
      
name: 🚀 Production deploy on release
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: cluster.hostliner.cz
        username: ${{ secrets.FTP_USERNAME_CLUSTER }}
        password: ${{ secrets.FTP_PASSWORD_CLUSTER }}
        local-dir: ./output/production/
        server-dir: czechvisual.space/sluno/newsletter/production/
        state-name: .production.json
